# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# Generated platform folders (regenerate with `flutter create .`)
# Comment these out once you commit real platform config.
/android/
/ios/
# /web/ is committed — it holds custom index.html/manifest/icons and is needed for `flutter build web` in CI.
/macos/
/windows/
/linux/

# Firebase
.firebase/
firebase-debug.log
*.log

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
